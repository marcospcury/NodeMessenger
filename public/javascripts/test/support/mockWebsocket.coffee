define ['backbone'], (Backbone)->
  class websocket extends Backbone.Model
    
    connect: ->

    notifyConversation: (contactId) ->

    emit: ->

    disconnect: ->

    sendMessage: (message) ->

  websocket