extends layout

block content
  span Apelido:
    input(type="text", id="autor")
    select(id="cor-texto")
      option Blue
      option Red
  #dialog-emoticon(title="Emoticons")
    #emoticons-container
  #dialog-conversation
    #conversation-history
    textarea(id='message-area')
    button(id="send-button") Enviar
    button(id="emoticon-button") Emoticons